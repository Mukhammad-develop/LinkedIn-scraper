# LinkedIn Scraper Configuration

# Browser Settings
HEADLESS_MODE=true
BROWSER_TIMEOUT=30
IMPLICIT_WAIT=10

# Rate Limiting
REQUEST_DELAY=2
MAX_RETRIES=3

# Output Settings
OUTPUT_FORMAT=json
OUTPUT_DIR=./data

# Proxy Settings (Optional)
USE_PROXY=false
PROXY_LIST=

# Database Settings (Step 8)
DATABASE_URL=sqlite:///linkedin_data.db

# API Settings (Step 14)
API_PORT=8000
API_HOST=localhost 